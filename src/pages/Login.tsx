import { AppLayout } from "@/components/layout/AppLayout";

const Login = () => (
  <AppLayout>
    <div className="p-6">
  <h1 className="text-3xl font-bold mb-2">Connexion</h1>
      <p className="text-muted-foreground">Page de connexion (placeholder).</p>
    </div>
  </AppLayout>
);

export default Login;
